import PlaceholderPage from '../components/PlaceholderPage';

export default function SalesReturn() {
  return <PlaceholderPage title="Sales Return" message="Record sales returns. Under development." />;
}
