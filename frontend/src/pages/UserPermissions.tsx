import PlaceholderPage from '../components/PlaceholderPage';

export default function UserPermissions() {
  return <PlaceholderPage title="User Permissions" message="Configure roles and permissions per user. Under development." />;
}
