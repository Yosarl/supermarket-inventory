import PlaceholderPage from '../../components/PlaceholderPage';

export default function BarcodePrint() {
  return <PlaceholderPage title="Barcode Print" message="Print barcode labels. Under development." />;
}
