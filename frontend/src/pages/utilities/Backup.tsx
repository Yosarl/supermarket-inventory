import PlaceholderPage from '../../components/PlaceholderPage';

export default function Backup() {
  return <PlaceholderPage title="Backup" message="Backup database and data. Under development." />;
}
