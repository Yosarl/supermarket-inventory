import PlaceholderPage from '../components/PlaceholderPage';

export default function StockAdjustment() {
  return <PlaceholderPage title="Stock Adjustment" message="Adjust stock quantities (plus/minus). Under development." />;
}
