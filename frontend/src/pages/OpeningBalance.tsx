import PlaceholderPage from '../components/PlaceholderPage';

export default function OpeningBalance() {
  return <PlaceholderPage title="Opening Balance" message="Set opening balances for ledger accounts. Under development." />;
}
